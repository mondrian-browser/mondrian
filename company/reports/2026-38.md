# Week 38, 2026

Written Monday 21 September 2026, covering the week ending Sunday 20 September. Reports
are named for the week they cover, so the next one is `2026-39.md` on 28 September. The
company is two days old and nearly everything below happened on 20 September, so the
waiting times here are hours rather than weeks.

## What each role did

Setup, meaning Lloyd with Claude in Cowork, did the most. The working folder became a real
repo with its history, the suite passed on Windows, the branch was renamed to main to
match CI, `company/` was written and the repo was published, closing A1 to A4. In the
evening the PRD was revised: Mondrian is a complete browser with three rendering tiers
replacing the raw escape hatch (ADR 0011), credentials stored per profile with steering
scoped per profile (ADR 0012), and a threat model. The four routines were created and D2
was answered later.

Engineering opened PR #1. The fixture suite was dying before a single check in the
routine's container, and the cause was a product bug, not test plumbing: boot awaited the
start page and exited the app when it failed to load, so offline or behind a proxy
Mondrian would not start at all. CI showed two further causes behind it, both from one
guard that passed the Linux sandbox and GPU flags only when running as root. The run also
found Linux CI had been red on main for nine consecutive runs unnoticed, because Windows
was green and the suite passed by hand everywhere.

Design wrote `company/design/2026-09-20-skinned-tier-tokens.md` and found four token gaps.
Three are milestone 2 work and were left alone on purpose. The fourth became PR #2: the
chrome used its corner token in four places and a literal in fifteen others, so the
destijl theme said square and the browser still rendered a rounded omnibox.

Marketing wrote `positioning.md` from the PRD, then rewrote it after the evening revision,
because the two things the first version leaned on hardest had become false.

Operations ran for the first time this morning. This report is that run.

One gap worth naming. The largest change on main this week, the corpus growing to 186
sites and 22,349 regions and the trees crossing the rules, was committed directly by Lloyd
and has no LOG entry, so the week cannot be reconstructed from the LOG alone.

## Open pull requests

PR #1, engineering, boot survives an unreachable start page, opened 03:20 this morning and
has waited under three hours. Green on Linux and Windows, mergeable. PR #2, design, the
destijl corner tokens, opened 04:11 and has waited under two hours. Green on Windows, red
on Linux, and the failure is not its own: the Linux job aborts with the SUID sandbox
error, the second of the three causes PR #1 fixes. PR #2 cannot go green until PR #1 is in
main, so PR #1 is the one to look at first.

CI also warns that actions/checkout@v4 and actions/setup-node@v4 target Node 20, now
deprecated and forced onto Node 24. Nothing is failing. It is a line for the next audit.

## Open decisions

D3, sole trader now against Pty Ltd now, open since 20 September, about one day. D4, the
code signing certificate, the same, and it sits behind D3 because the vetting needs a
registered business. D1 is answered and done, D2 is answered later, so no trade mark
filing is being prepared and operations will raise it again when a release date is in
sight or if C3 changes the name. Nothing has waited more than two weeks, which this report
will say plainly when it becomes true. D3 is the one that matters: D4, the ASIC and BAS
calendar dates and the trade mark timing all wait behind it.

## Due in the next 30 days

The weekly report each Monday: 28 September, then 5, 12 and 19 October. On 1 October the
dependency and licence audit, and marketing's positioning check against the PRD. Every
other calendar entry is unset and waits on D3, on a filing or on a purchase. One entry was
retired this morning, the GitHub token rotation, because D1 settled that the routines push
through the Claude GitHub App and no token is stored anywhere.

## NEXT.md

Milestone 1 is done through B6.6 with the stacked classifier at 74.8 percent on 186 sites,
B4's gate has not been called yet, and C1, the control socket, is the largest unblocked
engineering item.
